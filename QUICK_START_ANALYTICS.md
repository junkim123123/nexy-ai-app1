# 📊 Analytics 페이지 빠른 접속 가이드

## 현재 상태

✅ **확인 스크립트 실행 완료**
- 데이터베이스 파일이 아직 생성되지 않았습니다
- 첫 사용자 요청이 들어오면 자동으로 생성됩니다

## Analytics 페이지 접속 방법

### 1단계: Streamlit 앱 실행

터미널에서 다음 명령어 실행:

```bash
streamlit run streamlit_app.py --server.port 8590
```

또는:

```bash
python -m streamlit run streamlit_app.py --server.port 8590
```

### 2단계: Analytics 페이지 접속

브라우저에서 다음 URL로 접속:

```
http://localhost:8590/analytics?admin=1
```

**중요**: URL 끝에 `?admin=1`을 반드시 추가해야 합니다!

## 접속 후 확인할 수 있는 정보

1. **📊 주요 지표**
   - 총 분석 수
   - 모드 카드 클릭 수
   - 상담 요청 수
   - 전환율

2. **💬 상담 요청 목록**
   - 이메일 주소
   - 사용자 이름
   - 제품/쿼리 정보
   - 메시지 내용
   - 요청 시간
   - 상태 (pending, contacted 등)

3. **📈 통계 및 트렌드**
   - 인기 검색어
   - 분석 모드 분포
   - 카테고리별 트렌드
   - 리스크 요인 분석
   - 일일 활동 차트

## 문제 해결

### 포트 8590이 이미 사용 중인 경우

다른 포트 사용:
```bash
streamlit run streamlit_app.py --server.port 8591
```

그리고 접속:
```
http://localhost:8591/analytics?admin=1
```

### Analytics 페이지에 접근할 수 없는 경우

1. Streamlit 앱이 실행 중인지 확인
2. URL에 `?admin=1` 파라미터가 있는지 확인
3. 브라우저 콘솔에서 오류 메시지 확인

### 데이터베이스가 보이지 않는 경우

- 첫 사용자 요청이 들어와야 데이터베이스가 생성됩니다
- 테스트하려면 앱에서 상담 요청을 한 번 제출해보세요

## 빠른 확인 명령어

```bash
# 1. 스크립트로 확인
python check_requests.py

# 2. Streamlit 앱 실행
streamlit run streamlit_app.py --server.port 8590

# 3. 브라우저에서 접속
# http://localhost:8590/analytics?admin=1
```


